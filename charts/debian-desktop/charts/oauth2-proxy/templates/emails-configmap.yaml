{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: oauth2-proxy-emails
  labels:
    app: oauth2-proxy
data:
  emails.txt: |
{{- range .Values.config.authenticatedEmails }}
    {{ . }}
{{- end }}
{{- end }}
